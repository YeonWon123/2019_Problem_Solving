
#include<iostream>
#include<vector>
using namespace std;

vector<vector<long long>>cache;
vector<pair<int, int>> firstConst;
vector<pair<int, int>> secondConst;
int rowSize, colSize,conSize;

int main() {
	cin >> colSize;
	cin >> rowSize;
	cache = vector<vector<long long>>(2, vector<long long>(rowSize,1));
	cin >> conSize;
	firstConst = vector<pair<int, int>>(conSize);
	secondConst = vector<pair<int, int>>(conSize);
	for(int i = 0; i < )
}